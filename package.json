{
  "name": "atomik-css",
  "version": "1.0.0",

  "scripts": {
    "atomik:gen": "node scripts/gen-atomik-postcss.mjs",
    "atomik:watch": "node scripts/gen-atomik-postcss.mjs --watch",
    "build": "npm run atomik:gen && <ton-build>",
    "dev": "npm run atomik:watch"
  },
  "dependencies": {
    "chokidar": "^4.0.3",
    "sass": "^1.89.0"
  },
  "devDependencies": {
    "postcss": "^8.5.6",
    "postcss-scss": "^4.0.9"
  }
}
